<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Bạn có thể cung cấp cấu hình các cơ sở tin cậy (trust anchors) nếu cần thiết -->
        </trust-anchors>
    </base-config>
</network-security-config>
